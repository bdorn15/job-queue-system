# Required — read by docker compose
POSTGRES_USER=jqs
POSTGRES_PASSWORD=jqspw
POSTGRES_DB=jqsdb
JWT_SECRET=dev-only-change-me-openssl-rand-base64-32

# Optional — only for running services outside Docker
REDIS_HOST=localhost
REDIS_PORT=6379
DATABASE_URL=postgresql://jqs:jqspw@localhost:5432/jqsdb
